# `hamstik work attachment download`

Download an attachment to a file

### `key`

Work item key

Value: `KEY`

### `attachment_id`

Attachment id (UUID)

Value: `ATTACHMENT_ID`

### ``-o`, --output`

Output path (defaults to the attachment's file name in the current directory)


Supports: `--json`, `--no-input`
