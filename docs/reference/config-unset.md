# `hamstik config unset`

Remove a configuration value

### `key`

Configuration key to remove: `profile`, `organization`, `project`, `editor`, `pager`, `output`, `git_branch_template`, or `audit_log`

Value: `KEY`


Supports: `--json`, `--no-input`
