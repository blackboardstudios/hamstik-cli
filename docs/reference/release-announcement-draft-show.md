# `hamstik release announcement draft show`

Show the active announcement draft

### `release_id`

Release version id (UUID)

Value: `RELEASE_ID`

### `--project`

Project key (overrides context)


Supports: `--json`, `--no-input`
