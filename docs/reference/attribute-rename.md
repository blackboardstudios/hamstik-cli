# `hamstik attribute rename`

Rename a definition without changing its stable key or type

### `key`

Stable Attribute key

Value: `KEY`

### `--name`

New display name

### `--reason`

Optional audit reason

### `--idempotency-key`

Explicit idempotency key


Supports: `--json`, `--no-input`
